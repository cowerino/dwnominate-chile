e27ec21c4fbcb77f6dae4bde272ddc7b2fba254a 2026-08-06 16:15:22 -0400
experiment: restore RCINT2 unit-sphere projection (NEGATIVE RESULT, do not merge)

Tests the track-D hypothesis that REQ-004's removal of the roll-call midpoint
projection explains the C++/Fortran classification gap.

The source-level part of that finding stands and is confirmed: the reference DOES
constrain the midpoint, in RCINT2 (dwnom2004.f:3299), the grid search that calls
PROLLC2. REQ-004 audited PROLLC2, the callee, and concluded the caller was
unconstrained. Its supporting observation was also a column misread of
dwnom2004.f:729-730, which interleaves (spread, midpoint) per dimension.

The CAUSAL part is refuted. Restoring the projection at the search site, which also
re-enables the pre-existing "if (projected) break" analogue of the Fortran's exit
from the search loop, makes agreement WORSE on every axis.

Panel 692,839 votes, 25 sub-period units, quad, NITER=4, matched per-period seed,
identical arguments to dyn25_quad_out:

                        baseline      restored     Fortran
  classification         93.4921%     93.1255%     94.55%
  correct              647,750       645,210
  log-likelihood       -99,530.16   -109,008.79   -98,999.9
  w2                       0.5063       0.5363      0.5163
  beta                     5.9289       5.6789      5.954

The intervention verifiably took effect: midpoints outside the unit ball fall from
18.04% (2,336 of 12,952, max norm 3.6813) to 0.00% (max norm exactly 1.0000). So
this is not a no-op; the constraint bound and the fit got worse by 9,478 nats.

Conclusion: the 99.9% co-location of the gap with out-of-ball roll calls was
confounding, as the audit itself flagged. Out-of-ball roll calls are intrinsically
harder for both engines. REQ-004 reached an empirically better engine by invalid
reasoning. The reasoning must be corrected in the source comments and in BRIDGE.md;
the change itself should stand.

Restores site 1 only (rollcall_optimizer.cpp), the direct RCINT2 analogue. Sites 2
and 3 (dwnominate.cpp:828, :1115) rest on PROLLC2 and CUTPLANE claims that the audit
did not overturn, and clamping there would deviate from the reference.

Run: (clamp-restore run, reproduction package)

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

